import charaters from './charaters';
import items from './items';
import areas from './areas';
import creeps from './creeps';

interface eternalReturn {
  charaters: charaters;
  items: items;
  areas: areas;
  creeps: creeps;
}

export default eternalReturn;
